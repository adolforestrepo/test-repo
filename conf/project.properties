# Local properties file for configuring RSuite setup via groovy
# scripting

# Hotfolder for article (word) documents
ingestion.word.hotfolder.dir={not a real directory}

# Hotfolder for books (should be zip file)
ingestion.books.hotfolder.dir={not a real directory}

# Hotfolder for taxonomy spreadsheets (should be xlsx file)
ingestion.taxonomy.hotfolder.dir={not a real directory}

# Hotfolder for article (word) documents
ingestion.ctdo.hotfolder.dir=E:\\ATD\\hotfolders\\CTDO

# Hotfolder for books (should be zip file)
ingestion.tdbackcontent.hotfolder.dir=E:\\ATD\\hotfolders\\T+D Back Content

# Hotfolder for taxonomy spreadsheets (should be xlsx file)
ingestion.tpm.hotfolder.dir=E:\\ATD\\hotfolders\\TPM
